---
title: Secret Field
description: A masked credential input where empty means "keep the stored value".
links:
  doc: https://ark-ui.com/docs/forms/password-input
---

<ComponentPreview componentName="secret-field" />

## Usage

```tsx
import { SecretField } from "@kanzo-tech/ui";
```

```tsx
<SecretField hasStoredValue value={value} onValueChange={setValue} />
```

Built on [Password Input](/docs/forms/password-input), so the reveal toggle, its
accessible name and the disabled / invalid propagation all come from the machine.

## Stored value

A secret that already exists on the server never reaches the client, so there is nothing to
prefill: the field renders empty and submitting it empty means "keep what is stored". The
hint says so, and disappears the moment the user types — after that, the submitted value
really does replace the secret, and the hint would be a lie.

<ComponentPreview componentName="secret-field" fileName="example-stored-value" />

## Controlled

An API key is not a password. `ignorePasswordManagers` keeps 1Password and friends from
offering to save it.

<ComponentPreview componentName="secret-field" fileName="example-controlled" />

## Sizes

<ComponentPreview componentName="secret-field" fileName="example-sizes" />

## Invalid and disabled

<ComponentPreview componentName="secret-field" fileName="example-invalid" />

## API Reference

| Prop | Type | Default |
| --- | --- | --- |
| `value` | `string` | — |
| `defaultValue` | `string` | — |
| `onValueChange` | `(value: string) => void` | — |
| `hasStoredValue` | `boolean` | `false` |
| `storedPlaceholder` | `string` | `"Leave empty to keep current"` |
| `placeholder` | `string` | — |
| `revealable` | `boolean` | `true` |
| `size` | `"sm" \| "md" \| "lg"` | `"md"` |
| `disabled` | `boolean` | — |
| `invalid` | `boolean` | — |
| `required` | `boolean` | — |
| `readOnly` | `boolean` | — |
| `name` | `string` | — |
| `id` | `string` | — |
| `autoComplete` | `"current-password" \| "new-password"` | — |
| `ignorePasswordManagers` | `boolean` | — |
| `className` | `string` | — |
| `ref` | `Ref<HTMLInputElement>` | — |
