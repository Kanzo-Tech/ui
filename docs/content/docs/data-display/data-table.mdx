---
title: Data Table
description: A sortable, filterable, paginated table over TanStack Table — on its own entry point.
---

<Callout type="info">
**Data Table vs [Table](/docs/data-display/table):** `Table` is markup only — semantic elements,
tokenised chrome, no data layer, in the root barrel. This is that table plus TanStack Table for
sorting, filtering and pagination.

That is a real dependency, which is why it sits on its own entry point:

```tsx
import { DataTable } from "@kanzo-tech/ui/table";
```

`@tanstack/react-table` is an **optional peer**, so `import { Button }` keeps working for
everyone who has not installed it. Reach for `Table` when you are laying out rows you already
have; reach for `DataTable` when the user needs to sort, filter or page through them.
</Callout>

<ComponentPreview componentName="data-table" />

## Usage

`DataTable` lives on the `@kanzo-tech/ui/table` subpath, not the root barrel, because
`@tanstack/react-table` is an **optional** peer dependency. Keeping it off the root means
`import { Button } from "@kanzo-tech/ui"` never pulls TanStack into the bundle — only the
apps that actually render a data table pay for it.

```tsx
import { type ColumnDef, DataTable, sortableHeader } from "@kanzo-tech/ui/table";
```

Install the peer alongside the library:

```bash
pnpm add @tanstack/react-table
```

```tsx
<DataTable columns={columns} data={data} searchKey="name" />
```

`ColumnDef`, `Row` and `CellContext` are re-exported from the same subpath, so a consumer
types its columns without a direct TanStack import.

## Sorting and toolbar

Sorting is opt-in per column. TanStack marks every column sortable by default, but a column
only becomes clickable when its `header` is built with `sortableHeader` — a plain-string
header stays inert.

<ComponentPreview componentName="data-table" fileName="example-sortable" />

`searchKey` filters on a single column id and, together with `toolbarActions`, is what makes
the toolbar row appear at all; with neither, no toolbar is rendered. Pagination hides itself
whenever there is only one page.

## Empty state

<ComponentPreview componentName="data-table" fileName="example-empty" />

## Footer

Give a column a `footer` and the table renders a `<tfoot>` — totals, counts, a summary row.
Columns without one leave an empty footer cell; if no column defines a footer, no `<tfoot>` is
emitted at all.

<ComponentPreview componentName="data-table" fileName="example-footer" />

## API Reference

| Prop | Type | Default |
| --- | --- | --- |
| `columns` | `ColumnDef<TData, TValue>[]` | — |
| `data` | `TData[]` | — |
| `searchKey` | `string` | — |
| `searchPlaceholder` | `string` | `"Search…"` |
| `onRowClick` | `(row: TData) => void` | — |
| `toolbarActions` | `ReactNode` | — |
| `pageSize` | `number` | `20` |
| `empty` | `ReactNode` | `"No results."` |

`sortableHeader(label: ReactNode)` returns a TanStack header renderer — use it as a column's
`header`.
