---
title: Theme gallery
description: One screen drawn once per shipped theme, so a catalogue of names becomes something you can actually compare.
full: true
---

<PreviewIframe name="theme-gallery" title="Theme gallery showcase" className="h-[900px]" />

A list of theme names tells you nothing, and a row of swatches is barely better: it is a claim
about colours that says nothing about whether an interface built from them holds together. So this
draws the **same arrangement** — the one the [theme studio](/docs/showcases/theme-studio) previews,
imported rather than rebuilt — once per theme the library ships.

The comparison then happens where comparisons actually happen, which is between two screens rather
than between two palettes. A brand fill only looks right or wrong next to the surface it sits on
and the ink it carries.

## Each tile is a theme, not a picture of one

A tile is a `data-theme` attribute on a `div`:

```tsx
<div data-theme="nord">
  <YourScreen />
</div>
```

That is the whole mechanism. `[data-theme="nord"]` is an ordinary attribute selector — it matches
an *element*, not a document — so the theme's custom properties land on that div and inherit into
everything inside it. Sixteen themes cost sixteen attributes: **no iframe per tile, no stylesheet
swapping, no JavaScript.**

This is worth knowing beyond this page, because it is what lets a product theme a *region*: a
tenant-branded panel inside an otherwise neutral application is the same attribute in a different
place. The only thing it asks of the theme layer is that colours are never declared on `:root`
alone — a custom property inherits its **computed** value, so a `:root` declaration resolves once on
`<html>` and every tile would paint the same theme.

<Callout title="Which is a rule with a scar on it">
  `kanzo.css` opened with a bare `:root, [data-theme="kanzo"]` for a day. `:root` has the same
  specificity as `[data-theme="x"]`, so the tie broke on source order, and the imports are
  alphabetical — every theme sorting before `kanzo.css` was silently repainted in kanzo's colours,
  eleven of the sixteen, dark ones included. It is now `:root:not([data-theme])`, and two assertions
  in `themes.test.ts` hold it there.
</Callout>

## Light and dark are two sets, not a switch

The control in the header picks **which set of themes you are looking at**. It converts nothing,
because there is nothing to convert: a theme carries one `color-scheme` and light and dark are two
themes rather than two halves of one. See [a theme is one flat
block](https://github.com/kanzo-tech/kanzo-ui/blob/main/decisions/a-theme-is-one-flat-block.md).

## Editing is somewhere else

Each tile links to the studio with `?from=<name>`, and the link carries the **name** rather than the
values. The gallery is wearing every theme and could hand the whole document over, but then the link
would carry a copy of the stylesheet and the studio would open on whatever the gallery believed when
it rendered. A name makes the studio go and read the theme itself.
