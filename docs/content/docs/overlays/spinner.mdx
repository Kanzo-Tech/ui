---
title: Spinner
description: An indeterminate progress indicator — for waits whose length you cannot know.
---

<ComponentPreview componentName="spinner" />

## Usage

```tsx
import { Spinner } from "@kanzo-tech/ui";
```

It renders a single `<svg role="status">` with `aria-label="Loading"`, and takes no props of its
own beyond what an SVG takes. Size it with a utility (`size-4` is the default) and it inherits
`currentColor`, so it is the right colour inside a button, a muted caption or an alert without
being told.

## Indeterminate only

A spinner says *something is happening* and nothing else. Use it when you genuinely cannot know
how far along the work is — a request in flight, a query planning. The moment you can count
something, stop using it:

| You know | Use |
| --- | --- |
| Nothing but that work started | `Spinner` |
| How far along it is | [`Progress`](/docs/data-display/progress) |
| The shape of what is arriving | [`Skeleton`](/docs/overlays/skeleton) |

The last row is the one most often got wrong. If content is loading into a layout you already
know the shape of — a list, a card, a table row — a skeleton holds that shape and nothing
shifts when the data lands. A spinner in the same place throws the layout away and then puts it
back, which reads as a flash even when it is fast.

## Inside a button

`Button` renders its own via `isLoading`. Prefer that to composing the two by hand — it does
three things a hand-rolled version usually forgets:

- **Keeps the label in the layout**, hidden but still measured, so the button does not resize
  mid-click and move whatever sits next to it.
- **Keeps the label for screen readers** as `sr-only` text, so the control is still named while
  it is busy rather than announcing itself as "Loading".
- **Sets `aria-busy` and `aria-disabled` together**, so it stops accepting clicks *and* says why.

```tsx
<Button isLoading>Publishing</Button>
```

<Callout type="warn">
Never let a spinner be the only thing on screen for a wait you cannot bound. It has no way to
distinguish "still working" from "stalled", so pair a long operation with text that names what
is happening — as the middle example above does.
</Callout>
