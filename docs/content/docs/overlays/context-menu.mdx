---
title: Context Menu
description: The same menu, opened by right-click on a region instead of by a button.
links:
  doc: https://ark-ui.com/docs/overlays/context-menu
---

<Callout type="info">
**Context Menu vs [Menu](/docs/overlays/menu):** this *is* `Menu` — the component renders it and
changes only the trigger, from a clickable control to a right-clickable region. Every part,
prop and behaviour documented on Menu applies here unchanged.

Reach for it when the target is an area rather than a control: a row, a canvas, a file in a
tree. If a visible button opens it, you want `Menu`.
</Callout>

<ComponentPreview componentName="context-menu" />

## Usage

```tsx
import {
  ContextMenu,
  ContextMenuContent,
  ContextMenuItem,
  ContextMenuSeparator,
  ContextMenuShortcut,
  ContextMenuTrigger,
} from "@kanzo-tech/ui";
```

```tsx
<ContextMenu>
  <ContextMenuTrigger asChild>
    <div>Right-click here</div>
  </ContextMenuTrigger>
  <ContextMenuContent>
    <ContextMenuItem value="edit">Edit</ContextMenuItem>
  </ContextMenuContent>
</ContextMenu>
```

This is `Menu` with a context trigger — the sub-components are the same parts under different
names, so anything true of `MenuItem` is true of `ContextMenuItem`. The trigger positions the
menu at the pointer rather than against an anchor, and gets `cursor-default` so a right-
clickable region does not advertise itself as text.

A context menu should never be the only route to an action: right-click is not reachable by
keyboard on its own, so mirror the items in a toolbar `Menu`.

## Anatomy

```text
ContextMenu
├── ContextMenuTrigger
└── ContextMenuContent
    ├── ContextMenuGroup
    │   └── ContextMenuItem
    │       └── ContextMenuShortcut
    ├── ContextMenuSeparator
    └── ContextMenuSub
        ├── ContextMenuSubTrigger
        └── ContextMenuSubContent
```

## Submenu

<ComponentPreview componentName="context-menu" fileName="example-submenu" />

## API Reference

Every part forwards to its `Menu` counterpart unchanged — see the
[Menu API reference](/docs/overlays/menu#api-reference). `ContextMenuItem` takes the same
`variant` prop, and `ContextMenuGroup` the same `heading`.
