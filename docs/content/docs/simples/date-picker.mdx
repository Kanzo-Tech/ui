---
title: Date Picker
description: A text input paired with a calendar popover.
links:
  doc: https://ark-ui.com/docs/simples/date-picker
---

<ComponentPreview componentName="date-picker" />

## Usage

```tsx
import {
  DatePicker,
  DatePickerContent,
  DatePickerInput,
  DatePickerTrigger,
  DatePickerValue,
} from "@kanzo-tech/ui";
```

`DatePicker` is [Calendar](/docs/simples/calendar) with `inline` off: the same machine and
the same `Calendar*` grid parts, now inside a positioned popover. Compose the grid from the
calendar parts — only the input, trigger, content and timer are specific to the picker.

The value is Ark's `DateValue`, not a `Date`. For a plain ISO string in and out, use
[Date Field](/docs/simples/date-field), which wraps this component.

## Anatomy

```text
DatePicker
├── DatePickerInput          (input + calendar button, or…)
├── DatePickerTrigger        (…a button showing DatePickerValue)
└── DatePickerContent
    ├── CalendarView         (see Calendar for the grid parts)
    ├── DatePickerTimer
    └── DatePickerPresetTrigger
```

## With a trigger button

<ComponentPreview componentName="date-picker" fileName="example-with-trigger" />

## Range

<ComponentPreview componentName="date-picker" fileName="example-range" />

## API Reference

### DatePickerInput

| Prop | Type | Default |
| --- | --- | --- |
| `size` | `"sm" \| "md" \| "lg"` | `"md"` |
| `index` | `number` | `0` |

Renders `DatePicker.Control`, an `InputGroup` and the calendar trigger button for you.

`DatePicker` extends `DatePicker.Root`, so `value`, `defaultValue`, `onValueChange`,
`selectionMode`, `numOfMonths`, `min`, `max`, `locale`, `open`, `disabled` and `readOnly`
pass through. `positioning` defaults to `{ placement: "top" }`.
