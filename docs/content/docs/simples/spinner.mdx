---
title: Spinner
description: An indeterminate progress indicator.
---

<ComponentPreview componentName="spinner" />

## Usage

```tsx
import { Spinner } from "@kanzo-tech/ui";
```

`Button` renders one itself via `isLoading` — prefer that over composing them by hand, since it
also preserves the button width and sets `aria-busy`.
