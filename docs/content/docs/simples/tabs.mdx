---
title: Tabs
description: Switches between panels of related content.
links:
  doc: https://ark-ui.com/docs/simples/tabs
---

<Callout type="info">
**Tabs vs [Segment Group](/docs/forms/segment-group):** they look alike and share no semantics.
Tabs is `tablist` / `tabpanel` — it **reveals a panel**, and the panels are the point. Segment
Group is built on Ark's radio group and emits `role="radiogroup"` — it **selects a value**, and
is a form control.

The test: if the thing you are switching between is *content on this page*, use Tabs. If it is a
*value you would submit*, use Segment Group.
</Callout>

<ComponentPreview componentName="tabs" />

## Usage

```tsx
import { Tabs, TabsContent, TabsList, TabsTrigger } from "@kanzo-tech/ui";
```

```tsx
<Tabs defaultValue="overview">
  <TabsList>
    <TabsTrigger value="overview">Overview</TabsTrigger>
  </TabsList>
  <TabsContent value="overview">…</TabsContent>
</Tabs>
```

`lazyMount` and `unmountOnExit` both default to `true`, so an inactive panel costs nothing —
it is never mounted, and it is torn down when you leave it. Pass `false` if a panel holds
state that has to survive a switch.

## Anatomy

```text
<Tabs>
  <TabsList>          ← renders its own indicator
    <TabsTrigger />
  <TabsContent />
```

`TabsList` renders the sliding indicator itself; there is no `TabsIndicator` to place.

## Variants

<ComponentPreview componentName="tabs" fileName="example-variants" />

## Vertical

Orientation lives on the root and reaches every part through `data-orientation`, so the list
and the panels reflow together.

<ComponentPreview componentName="tabs" fileName="example-vertical" />

## API Reference

### TabsList

| Prop | Type | Default |
| --- | --- | --- |
| `variant` | `"default" \| "underline"` | `"default"` |

### Tabs

| Prop | Type | Default |
| --- | --- | --- |
| `lazyMount` | `boolean` | `true` |
| `unmountOnExit` | `boolean` | `true` |

Everything else — `value`, `defaultValue`, `onValueChange`, `orientation`, `activationMode` —
is Ark's `Tabs.Root`. `useTabs` re-exports Ark's `useTabsContext` for reading the active tab
from a descendant.
